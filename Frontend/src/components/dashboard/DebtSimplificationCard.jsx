import { ArrowRight, CheckCircle2, Sparkles } from 'lucide-react'
import { Avatar } from '../ui/Avatar'
import { people } from '../../data/seedData'
export function DebtSimplificationCard({ transfers }) { return <section className="rounded-2xl border border-[#dce9dd] bg-[#f0f8f0] p-5"><div className="mb-4 flex items-start justify-between"><div><div className="mb-1 flex items-center gap-2"><Sparkles size={16} className="text-[#398451]"/><h2 className="font-bold text-[#284633]">Simplified debts</h2></div><p className="text-xs text-[#6e8274]">Min-cash-flow found the shortest way to settle up.</p></div><CheckCircle2 className="text-[#4a9a61]" size={19}/></div>{transfers.length ? <div className="space-y-2">{transfers.slice(0, 3).map((transfer) => { const from = people.find((p) => p.id === transfer.from); const to = transfer.to === 'you' ? { initials: 'MN', color: '#b8d9bd', name: 'You' } : people.find((p) => p.id === transfer.to); return <div className="flex items-center gap-2 rounded-xl border border-[#dfece0] bg-white/70 px-3 py-2.5" key={`${transfer.from}-${transfer.to}`}><Avatar initials={from?.initials || 'MN'} color={from?.color || '#b8d9bd'} size="sm"/><span className="text-xs font-semibold text-[#52665a]">{from?.name.split(' ')[0] || 'You'}</span><ArrowRight size={14} className="text-[#93a99a]"/><Avatar initials={to?.initials || 'MN'} color={to?.color || '#b8d9bd'} size="sm"/><span className="text-xs font-semibold text-[#52665a]">{to?.name?.split(' ')[0] || 'You'}</span><strong className="ml-auto text-sm text-[#28663c]">${transfer.amount.toFixed(2)}</strong></div> })}</div> : <p className="text-sm text-[#587261]">Everyone is settled up 🎉</p>}</section> }
