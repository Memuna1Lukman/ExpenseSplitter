import { Filter } from 'lucide-react'
import { ExpenseItem } from './ExpenseItem'
export function ExpenseList({ expenses, title = 'Recent expenses' }) { return <section className="rounded-2xl border border-[#e3eae2] bg-white p-5 shadow-[0_4px_20px_rgba(38,59,45,.04)]"><div className="mb-2 flex items-center justify-between"><div><h2 className="font-bold text-[#25382b]">{title}</h2><p className="mt-1 text-xs text-[#8a978d]">Your latest shared spending</p></div><button className="flex items-center gap-1.5 rounded-lg border border-[#e5ebe4] px-2.5 py-2 text-xs font-semibold text-[#718077] hover:bg-[#f5f8f4]"><Filter size={13}/> Filter</button></div>{expenses.length ? expenses.map((expense) => <ExpenseItem key={expense.id} expense={expense}/>) : <p className="py-10 text-center text-sm text-[#8a978d]">No expenses yet. Add your first one above.</p>}</section> }
